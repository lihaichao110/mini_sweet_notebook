import { HIconType } from '@/types/icons'

interface MenuIconsType {
  icon: HIconType
  name: string
}

/** menu 菜单 */
export const MenuIcons: MenuIconsType[] = [
  { name: '餐饮', icon: 'tianjia' },
  { name: '购物', icon: 'wode-F' },
]
